import Intent from "@src/common/Badge/components/Intent";
import Outline from "@src/common/Badge/components/Outline";

const variants = {
    intent: Intent,
    outline: Outline
}

export default variants