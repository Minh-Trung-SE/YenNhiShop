import Intent from "@src/common/Button/components/Intent";
import Outline from "@src/common/Button/components/Outline";

const variants = {
    intent: Intent,
    outline: Outline
}

export default variants