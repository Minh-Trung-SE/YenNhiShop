import {Portal, Trigger, Root, Arrow, Content, Provider, } from '@radix-ui/react-tooltip';
import Default from "@src/common/Tooltip/components/Default";

const Tooltip = {
    Portal,
    Provider,
    Root,
    Trigger,
    Arrow,
    Content,
    Default
}

export default Tooltip;